import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.aldar.app',
  appName: 'الدار نت',
  webDir: 'dist',
  server: {
    url: 'https://aligamal89901-svg.github.io/aldar-net/',
    cleartext: false,
  },
};

export default config;
